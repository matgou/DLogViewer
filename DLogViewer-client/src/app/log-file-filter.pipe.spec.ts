import { LogFileFilterPipe } from './log-file-filter.pipe';

describe('LogFileFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LogFileFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
