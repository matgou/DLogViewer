import { MessagesFilterPipe } from './messages-filter.pipe';

describe('MessagesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MessagesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
